# 答辩问答预判 —— 老师大概率会问的C语言问题

---

## 一、指针相关

### Q1：你代码里大量使用了指针，能解释一下指针是什么吗？为什么用指针传参而不是直接传值？

**标准答案**：

指针就是一个**存放内存地址的变量**。比如`int *p`中的p不是存整数，而是存"某个整数在内存里的位置"。

我用指针传参主要有两个原因：

**第一，效率**。我的Pokemon结构体有几十个字段，如果直接传值，函数调用时会复制整个结构体（可能几百字节），非常慢。传指针只传8个字节的地址，效率极高。

**第二，函数内修改外部变量**。比如：
```c
void InitPokemon(Pokemon *p, int speciesId, int level) {
    p->level = level;   // 通过指针修改外部的Pokemon变量
    p->currentHP = p->maxHP;
}
```
如果我传的是值而不是指针，函数里修改的只是一个副本，外部的原始变量完全不受影响。

**`.`和`->`的区别**：如果变量本身是结构体，用`.`访问成员；如果是指向结构体的指针，用`->`访问成员。`p->level`等价于`(*p).level`。

---

### Q2：你的函数里有些参数是`const PokemonType *`，加const是什么意思？

**标准答案**：

`const`表示"只读"，告诉编译器"我不会通过这个指针修改数据"。

比如查询函数：
```c
const MoveData *GetMoveData(const char *name);
```
返回`const MoveData *`意味着调用者拿到这个指针后只能读数据，不能修改技能数据库的内容。

这样做有两个好处：
1. **防止意外修改**：如果我不小心写了`result->power = 999`，编译器会直接报错
2. **自我说明**：一看函数声明就知道它是只读查询，不会修改任何数据

---

### Q3：`memset(p, 0, sizeof(*p))`这行代码是什么意思？为什么要这样做？

**标准答案**：

`memset`是C标准库函数，作用是**把一块内存的所有字节设成同一个值**。

这行代码的意思是：把p指向的结构体从头到尾全部填0。

`sizeof(*p)`是计算p指向的那个结构体占多少字节。所以不管这个结构体有多大，一行`memset`就能完整清零。

**为什么要这样做**：结构体有很多字段，如果我只在初始化时设置了其中几个，没设置的字段会带着内存里的**随机垃圾值**，后续使用可能导致奇怪的bug。memset清零是最安全的初始化方式。

---

### Q4：`attacker->move_power[moveIdx]`这行代码里，`->`和`[]`的执行顺序是怎样的？

**标准答案**：

`->`和`[]`的优先级相同，结合性是从左到右。

所以`attacker->move_power[moveIdx]`的执行顺序是：
1. `attacker->move_power`：先通过指针访问结构体的move_power成员（它是一个数组）
2. `[moveIdx]`：再取这个数组的第moveIdx个元素

它等价于`(*attacker).move_power[moveIdx]`。

---

## 二、结构体相关

### Q5：你定义了很多结构体，结构体和数组有什么区别？为什么要用结构体？

**标准答案**：

**数组**：存的是**同一种类型**的多个数据。比如`int scores[10]`存10个int。

**结构体**：可以把**不同类型**的数据打包在一起。比如一个Pokemon结构体里既有int（等级、HP），又有char数组（名字），又有枚举（属性类型），还有纹理（Texture2D）。

结构体的优势是**逻辑内聚**——所有和一只宝可梦相关的数据放在一起，用一个变量就能代表一只完整的宝可梦。这比用分散的独立变量清晰得多。

另外，C语言支持**结构体直接赋值**：
```c
p->moves[p->moveCount] = *md;  // 把md指向的整个MoveData结构体复制过来
```
这在初始化时特别方便。

---

### Q6：你代码里有一个`PokemonType`枚举类型，它和直接写int数字有什么区别？

**标准答案**：

```c
typedef enum {
    TYPE_NORMAL, TYPE_FIRE, TYPE_WATER, ...
} PokemonType;
```

枚举有三个好处：

1. **可读性**：写`TYPE_FIRE`比写数字`1`好理解得多，代码就是文档
2. **类型检查**：如果用int，编译器不会阻止你写一个不存在的属性值（比如999）。用枚举后，编译器可以给出警告
3. **用作数组下标**：因为枚举值本质就是int（TYPE_FIRE=1, TYPE_WATER=2...），可以直接当数组索引用——我的属性克制表`typeChart[TYPE_FIRE][TYPE_WATER]`就是这样用的

---

## 三、数组相关

### Q7：你的属性克制表用了`float typeChart[19][19]`二维数组，二维数组在内存中是怎么存的？

**标准答案**：

C语言的二维数组在内存中是**按行连续存储**的，就是一行接一行地排。

`typeChart[19][19]`在内存中是一个长度为361（19×19）的一维连续空间。`typeChart[i][j]`的地址计算公式是：`首地址 + (i × 19 + j) × sizeof(float)`。

访问`typeChart[2][3]`时，程序计算偏移量=2×19+3=41，然后读第41个float。

这也是为什么二维数组的两个维度都必须指定大小（第一维其实是可省的，但我明确写了19）。

---

### Q8：你代码里`playerPoke.move_pp[cursorPos]--`做了什么？数组下标越界会怎样？

**标准答案**：

`move_pp[cursorPos]--`做了两件事：先取move_pp数组的第cursorPos个元素，然后把它减1。

这表示：玩家选中一个技能后，该技能的PP值扣掉1点。

**数组下标越界**是C语言的常见问题。如果cursorPos不小心变成了5（而数组只有4个元素），C语言**不会报任何错误**！它会直接读写数组后面的内存区域，可能导致：
- 读到垃圾数据
- 覆盖其他变量的值
- 程序崩溃（访问了受保护的内存）

所以使用数组前一定要检查下标范围。我在技能选择代码中就有检查：`if (cursorPos >= playerPoke.move_count) break;`

---

## 四、动态内存相关

### Q9：你用了malloc分配内存，malloc和直接声明数组有什么区别？

**标准答案**：

| | `int arr[100]`（栈数组） | `malloc`（堆内存） |
|---|---|---|
| 内存位置 | 栈（stack） | 堆（heap） |
| 大小 | 编译时必须确定 | 运行时动态决定 |
| 生命周期 | 离开作用域自动释放 | 手动调用free才释放 |
| 大小限制 | 栈空间有限（一般几MB） | 可分配很大 |

我的使用场景：加载宝可梦数据库时，我不知道有多少只宝可梦，所以必须用malloc根据实际数据量动态分配。

**malloc必须配对free**：每次malloc都要在适当的时候调用free，否则会造成内存泄漏——程序占用的内存越来越多但不归还给系统。

---

### Q10：malloc返回的是什么类型？为什么你要强制类型转换？

**标准答案**：

malloc的原型是`void *malloc(size_t size)`，返回`void *`类型。

`void *`是"通用指针"，可以指向任何类型，但不能直接用`*`解引用或做指针算术。

所以我需要把`void *`转成目标类型：
```c
speciesDB = (SpeciesData *)malloc(sizeof(SpeciesData) * speciesCount);
```

这样`speciesDB[i]`才能正确访问——编译器需要知道每个元素是`SpeciesData`类型（占多少字节），才能计算偏移量。

> 注意：纯C语言中，`void *`会自动转换，强制类型转换其实可以不写。但写了更清晰，也是好习惯。

---

### Q11：如果malloc失败了怎么办？你的代码有处理吗？

**标准答案**：

malloc失败时返回NULL（空指针）。如果不检查直接使用NULL指针，程序会崩溃。

我在`ReadFileText`函数中有处理：
```c
char *buf = (char *)malloc(size + 1);
if (buf) {            // buf不为NULL才继续
    fread(buf, 1, size, f);
    buf[size] = '\0';
}
```

但是诚实地讲，我在加载数据库的`LoadPokemonDB`函数中，对`malloc(speciesDB)`没有显式检查NULL，这是一个可以改进的地方。在实际答辩现场可以坦诚指出这一点。

---

## 五、文件操作相关

### Q12：你的程序需要读外部文件，请讲解一下C语言读文件的过程？

**标准答案**：

我的`ReadFileText`函数完整展示了读文件的标准流程：

```c
FILE *f = fopen(path, "rb");     // ①打开文件
fseek(f, 0, SEEK_END);           // ②移动到末尾
long size = ftell(f);            // ③获取文件大小
fseek(f, 0, SEEK_SET);           // ④回到开头
char *buf = malloc(size + 1);    // ⑤分配内存
fread(buf, 1, size, f);          // ⑥读入数据
buf[size] = '\0';                // ⑦加结束符
fclose(f);                       // ⑧关闭文件
```

逐步解释：
1. **fopen**：参数"rb"——r=读，b=二进制模式。文本模式会转换换行符，二进制模式原样读取
2. **fseek + ftell**：获取文件大小的经典组合。fseek移动"文件指针"（当前读写位置），ftell返回当前位置的字节偏移
3. **fread**：一次性把整个文件读到内存。参数含义是：读size次，每次读1字节
4. **buf[size] = '\0'**：手动添加C字符串结束符。fread不会自动加，必须手动加
5. **fclose**：关闭文件，释放系统资源。**绝对不能忘记！**

---

### Q13：为什么用"rb"而不是"r"？二进制模式和文本模式有什么区别？

**标准答案**：

`"r"`是文本模式，`"rb"`是二进制模式。

主要区别在Windows系统上：
- **文本模式**：读到`\r\n`时会自动转成`\n`，写入时反过来。还会在读到`0x1A`（Ctrl+Z）时认为文件结束
- **二进制模式**：原样读取，不做任何转换

我读的资源文件可能包含图片数据或UTF-8的中文字符，用二进制模式可以保证数据不被修改。

---

## 六、循环与条件判断

### Q14：你的for循环里经常出现多个条件，比如`for (int i = 0; i < sp->moveCount && p->moveCount < 4; i++)`，为什么要两个条件？

**标准答案**：

这个for循环同时受两个条件限制：

- `i < sp->moveCount`：不能超过数据库里记录的技能数量
- `p->moveCount < 4`：每个宝可梦最多只能有4个技能（`MAX_MOVES = 4`）

两个条件用`&&`（逻辑与）连接，其中一个不满足就会停止循环。这样不管数据库里配了多少技能，每只宝可梦最多只装4个，防止数组越界。

这是**防御性编程**的体现——多一层保护总没错。

---

### Q15：switch-case里为什么要写break？不写会怎样？

**标准答案**：

switch-case有一个特性叫**"穿透"（fall-through）**：一旦匹配到某个case，会一直执行到遇到break或switch结束为止。

如果不写break：
```c
switch (state) {
    case GAME_TITLE:
        // 也会执行到 GAME_STORY 的代码！
    case GAME_STORY:
        // 也会执行到 GAME_DIALOGUE 的代码！
    case GAME_DIALOGUE:
        ...
}
```
三个case的代码会全部执行，这显然不是我们想要的。

但也有**故意利用穿透**的情况，比如：
```c
case DIR_LEFT:
case DIR_RIGHT:
    p->animFrames = map->animLeft;  // 左右方向共用同一套动画
    break;
```
这里DIR_LEFT匹配后穿透到DIR_RIGHT，两个方向做同样的处理。

---

## 七、字符串操作

### Q16：你代码里用了`strcmp`比较字符串，为什么不能用`==`？

**标准答案**：

C语言中，字符串是字符数组，数组名代表的是**首地址**。

```c
char name1[] = "喷火龙";
char name2[] = "喷火龙";
if (name1 == name2)  // 比较的是两个数组的地址！永远不相等！
```

`==`比较的是指针的值（内存地址），而不是字符串内容。两个不同数组的地址肯定不同，即使内容一样也会返回false。

`strcmp`是专门比较字符串内容的函数：
- 返回0：两个字符串相等
- 返回负数：第一个字符串字典序更小
- 返回正数：第一个字符串字典序更大

---

### Q17：`snprintf`和`sprintf`有什么区别？为什么你用`snprintf`？

**标准答案**：

`sprintf(buf, "No.%d", id)` 不检查缓冲区大小，如果生成的字符串超过buf容量，会**溢出覆盖后面的内存**（缓冲区溢出漏洞）。

`snprintf(buf, sizeof(buf), "No.%d", id)` 多了第二个参数，指定缓冲区最大容量。如果生成的字符串太长，`snprintf`会自动截断，保证不会越界写入。

**使用`snprintf`是安全编程的好习惯**，防止潜在的缓冲区溢出问题。

---

## 八、随机数与游戏逻辑

### Q18：你用了`rand()`生成随机数，`rand() % 16`是什么意思？随机数可靠吗？

**标准答案**：

`rand()`返回0到RAND_MAX（通常是32767）之间的伪随机整数。

`rand() % 16`就是取除以16的余数，结果范围是0~15，共16种可能。

我的用法举例：
```c
int rnd = 85 + (rand() % 16);   // 伤害波动：85~100
int crit = (rand() % 16 == 0);  // 暴击概率：1/16
```

**关于随机数的可靠性**：`rand()`是伪随机，每次程序运行会产生相同的随机序列。我在InitBattle中调用了`srand((unsigned int)time(NULL))`，用当前时间作为种子，这样每次运行的结果就不同了。

---

## 九、static关键字

### Q19：你的代码里很多变量和函数前面加了`static`，有什么作用？

**标准答案**：

`static`在不同的上下文中有不同的含义：

**在文件作用域（全局变量/函数前）**：
```c
static GameState currentState;  // 只能在game.c内部访问
static void DrawBackground(void);  // 只能在battle.c内部调用
```
作用是**限制作用域**——只在当前.c文件内可见，其他文件无法访问。这是C语言的封装机制，类似其他语言的private。

**在函数内部（局部变量前）**：
```c
void someFunc(void) {
    static int callCount = 0;  // 只会初始化一次
    callCount++;
}
```
作用是**延长生命周期**——变量不随函数返回而销毁，下次调用时保留上次的值。

我在项目中主要用第一种——让全局变量和内部函数只在当前文件内可见，防止被其他文件意外修改。

---

## 十、综合理解

### Q20：你的战斗系统是如何保证回合顺序的？万一双方同时出手怎么办？

**标准答案**：

我的战斗系统是**严格回合制**，不是实时制。双方不会同时出手。

实现方式是**状态机**：
1. STATE_EXECUTE中，`animPhase=0`先处理玩家的攻击
2. 玩家攻击动画播完（animTimer>30）后，才进入`animPhase=2`处理敌方反击
3. 敌方攻击播完后，才会回到STATE_COMMAND让玩家继续操作

整个流程是**串行的**，不存在并发问题。我甚至用了一个`goto enemyTurn`来在玩家未命中时直接跳到敌方回合，保证流程的完整性。

---

### Q21：如果你的程序要加一个新功能——比如背包系统，你需要改哪些文件？

**标准答案**：

这考察的是对模块架构的理解：

1. **`item.h` / `item.c`**：把现在空的item.c实现，定义物品结构体和背包操作函数（增删查改）
2. **`battle.c`**：在STATE_COMMAND中case 1（道具选项）目前只是显示"背包空空如也"，需要改为实际的道具使用逻辑
3. **`game.c`**：可能需要在GameState中加一个GAME_BAG背包界面状态

**不需要改的文件**：player.c（玩家移动和背包无关）、pokemon.c（宝可梦数据结构不需要变）、ui.c（通用UI函数可以复用）

这体现了**模块化设计**的优势——加新功能只需要改相关的几个文件。

---

