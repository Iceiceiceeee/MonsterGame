# 答辩口述稿（直接照着说）

---

## 开场白（约30秒）

各位老师好，我今天答辩的项目是一个**用C语言开发的宝可梦风格回合制对战游戏**。

这个程序总共约**3000行纯C代码**，使用了**raylib图形库**来做画面渲染。整个游戏包含了**标题界面、故事开场、2D世界地图漫游、回合制战斗系统、宝可梦图鉴浏览**等完整功能。

接下来，我将从**程序整体架构、核心C语言技术点、以及程序运行流程**三个方面来介绍我的项目。

---

## 第一章：程序整体架构（约2分钟）

### 1.1 模块划分

我的程序采用了**多文件模块化设计**，一共7个核心C源文件，各司其职：

**main.c** 是程序的入口，只有30行代码，负责创建窗口、启动主循环。

**game.c** 是整个程序的"大脑"，约1100行，用**状态机**管理7种游戏状态（标题、故事、对话、世界地图、战斗、图鉴、制作人员），所有状态切换都在这里完成。

**battle.c** 是战斗系统，约1200行，是代码量最大、逻辑最复杂的模块。它实现了完整的回合制对战：包括属性克制、命中判定、暴击、伤害计算，还有一个消息驱动的状态机来控制回合内的执行顺序。

**pokemon.c** 负责宝可梦个体的初始化和伤害计算。

**pokemon_db.c** 负责从外部文件读取宝可梦数据库，提供查询接口。

**player.c** 负责玩家在地图上的移动、碰撞检测和动画切换。

**ui.c** 提供可复用的UI绘制函数，比如面板、进度条、居中文字。

### 1.2 我使用了哪些C语言核心技术

回顾整个项目，我重点运用了以下C语言知识点：

1. **结构体（struct）和枚举（enum）** —— 定义了Pokemon、Player、BattleContext等核心数据结构
2. **指针** —— 函数传参、动态内存管理、数组遍历全部依赖指针
3. **动态内存分配（malloc/free）** —— 加载数据库时根据文件大小动态申请内存
4. **文件读写（fopen/fread/fclose）** —— 读取游戏资源文件
5. **二维数组** —— 实现19×19的属性克制表
6. **状态机（switch-case）** —— 管理游戏和战斗的复杂状态切换
7. **字符串操作（strcmp/strcpy/snprintf）** —— 文本处理、路径拼接

接下来我会结合关键代码，逐一讲解这些技术点。

---

## 第二章：核心C语言技术点讲解（约5分钟）

### 2.1 程序入口与主循环 —— main.c

请大家看main函数，这是我程序的骨架：

```c
int main(void)
{
    InitWindow(960, 640, "Monster Game");  // 创建窗口
    SetTargetFPS(60);                        // 限制60帧
    InitGame();                               // 加载资源

    while (!WindowShouldClose())             // 主循环
    {
        UpdateGame();    // 更新逻辑
        BeginDrawing();  // 开始画
        DrawGame();      // 画画面
        EndDrawing();    // 显示
    }

    CloseGame();    // 释放资源
    CloseWindow();  // 关闭窗口
    return 0;
}
```

这里有两个关键点要解释：

第一，**`while (!WindowShouldClose())`** 构成了游戏的生命线。`WindowShouldClose()`是raylib提供的函数，当用户点击窗口关闭按钮时返回true。前面加感叹号表示"只要用户没关窗口，就一直循环"。这就是所有游戏通用的"主循环"模式。

第二，循环体里每一帧做三件事——**更新逻辑、绘制画面、显示画面**。这是游戏编程最核心的"Update-Draw"模式。60FPS意味着每秒执行60次这个循环。

### 2.2 状态机设计 —— game.c

接下来讲我的状态机设计。游戏在任何时刻只处于一种状态，我用**枚举（enum）**定义了7种状态：

```c
typedef enum {
    GAME_TITLE,     // 标题界面
    GAME_STORY,     // 故事剧情
    GAME_DIALOGUE,  // 角色对话
    GAME_CREDITS,   // 制作人员
    GAME_BATTLE,    // 战斗
    GAME_POKEDEX,   // 图鉴
    GAME_WORLD      // 世界地图
} GameState;
```

**枚举的好处**：我用`GAME_BATTLE`这个有意义的单词来代表状态，比用数字0、1、2清晰得多。编译器会自动给它们编号，枚举值还能直接当整数用。

在UpdateGame函数中，我用**`switch-case`**根据当前状态分发不同的处理逻辑：

```c
switch (currentState) {
    case GAME_TITLE:
        // 检测鼠标是否点击按钮
        if (鼠标点击开始游戏)
            currentState = GAME_STORY;  // 状态切换
        break;
    case GAME_WORLD:
        UpdatePlayer(&worldPlayer, &worldMap, dt);  // 更新玩家
        break;
    case GAME_BATTLE:
        UpdateBattle(&battleCtx);
        if (IsBattleFinished())
            currentState = GAME_WORLD;  // 战斗结束回地图
        break;
}
```

**switch-case的两个要点**：
- 每个case后面必须写`break`，否则会"穿透"到下一个case继续执行，这会导致逻辑错误
- 所有可能的状态都要有对应的case，否则程序会漏掉某些情况

### 2.3 结构体与指针 —— 核心数据组织方式

我大量使用了**结构体（struct）**来组织数据。比如宝可梦这个核心概念：

```c
typedef struct {
    int speciesId;          // 图鉴编号
    char name[32];          // 名字
    int type1, type2;       // 第一属性、第二属性
    int level;              // 等级
    int currentHP, maxHP;   // 当前HP、最大HP
    int attack, defense;    // 攻击力、防御力
    int spAttack, spDefense;// 特攻、特防
    int speed;              // 速度
    int currentExp;         // 当前经验
    MoveData moves[4];      // 4个技能（结构体数组）
    int movePP[4];          // 每个技能的PP值
    Texture2D frontSprite;  // 正面贴图
    Texture2D backSprite;   // 背面贴图
} Pokemon;
```

**结构体的三个好处**：
1. **把相关的数据打包在一起**，一个Pokemon变量就包含了它所有的信息
2. **可以用`.`或`->`直接访问成员**，比如`pikachu.currentHP -= 30`扣30点血
3. **结构体之间可以直接赋值**，比如`pokemon.moves[0] = moveData`，一行代码把整个技能结构体复制过去

访问结构体成员时，**`.`和`->`的区别**是C语言必考点：
- 如果变量是结构体本身，用`.`：`pokemon.level`
- 如果变量是指向结构体的指针，用`->`：`ptr->level`，它等价于`(*ptr).level`

我的函数几乎全用指针传参，因为：
- 结构体很大（Pokemon结构体有几十个字段），传指针只传8字节地址，效率远高于复制整个结构体
- 函数内可以通过指针修改外部变量的值

### 2.4 动态内存管理 —— malloc/free

游戏启动时需要加载宝可梦数据库，但我事先不知道数据库里有多少只宝可梦。这时就不能用固定大小的数组，需要用**动态内存分配**。

我先写了一个读取文件到内存的函数：

```c
static char *ReadFileText(const char *path) {
    FILE *f = fopen(path, "rb");        // 1.打开文件
    if (!f) return NULL;

    fseek(f, 0, SEEK_END);              // 2.跳到文件末尾
    long size = ftell(f);               // 3.获取文件大小
    fseek(f, 0, SEEK_SET);              // 4.回到开头

    char *buf = (char *)malloc(size + 1); // 5.分配堆内存
    fread(buf, 1, size, f);             // 6.一次性读入
    buf[size] = '\0';                   // 7.加字符串结束符
    fclose(f);                          // 8.关闭文件
    return buf;
}
```

**这段代码展示了C语言文件操作的全流程**：
- `fopen/fclose`：打开和关闭文件，参数"rb"表示二进制只读
- `fseek/ftell`：移动文件指针来获取文件大小
- `malloc`：在堆上动态分配内存，参数是字节数
- `fread`：把文件内容读到内存缓冲区中

获取到文件内容后，我根据解析出的数据条数，用`malloc`分配刚好够用的数组空间。程序退出时，调用`free()`释放这些内存。

**关于malloc有两个注意事项**：
一是malloc返回`void*`类型，需要强制转换成目标类型，比如`(SpeciesData *)malloc(...)`
二是每次malloc必须有对应的free，否则会造成**内存泄漏**。我的程序在`UnloadPokemonDB`函数中统一释放。

### 2.5 二维数组实现属性克制表 —— battle.c

宝可梦战斗的核心机制是**属性克制**——火克草、水克火、电克水等等，共18种属性，两两之间存在克制关系。

我用一个**19×19的二维数组**来存储这个克制表：

```c
static float typeChart[19][19];

// 初始化：全部设为1.0（普通效果）
for (int i = 0; i < 19; i++)
    for (int j = 0; j < 19; j++)
        typeChart[i][j] = 1.0f;

// 设置克制关系
typeChart[TYPE_FIRE][TYPE_GRASS]    = 2.0f;  // 火克草，2倍伤害
typeChart[TYPE_FIRE][TYPE_WATER]    = 0.5f;  // 火打水，减半
typeChart[TYPE_NORMAL][TYPE_GHOST]  = 0.0f;  // 一般打幽灵，无效
```

这里的关键设计是**用枚举值作为数组下标**。枚举值在底层就是整数（TYPE_FIRE=1, TYPE_WATER=2...），可以直接当索引用。要查火属性打草属性的倍率，一行代码就够了：`typeChart[TYPE_FIRE][TYPE_GRASS]`，这种"查表法"比写几百个if-else高效得多。

获取克制倍率时，还要考虑双属性宝可梦：

```c
static float GetTypeMultiplier(int atkType, int defType1, int defType2) {
    float m = typeChart[atkType][defType1];     // 第一属性
    if (defType2 != TYPE_NONE)
        m *= typeChart[atkType][defType2];       // 第二属性乘上去
    return m;
}
```

举个例子：火系技能打"草+虫"双属性的宝可梦，倍率=2.0×2.0=4倍伤害！

### 2.6 战斗伤害计算公式

伤害计算是战斗系统的核心算法，我实现了标准的宝可梦伤害公式：

```c
// 基础伤害 = f(等级, 威力, 攻击力, 防御力)
int base = (int)(((2.0 * level / 5.0 + 2.0) * power * atk / def) / 50.0) + 2;

// 随机数 85%~100%
int rnd = 85 + (rand() % 16);

// 暴击判定 1/16概率，1.5倍伤害
int crit = (rand() % 16 == 0) ? 1.5 : 1.0;

// 最终伤害 = 基础值 × 属性克制 × 随机波动 × 暴击
int final = (int)(base * typeMultiplier * (rnd / 100.0f) * crit);
```

这个公式涉及**几个C语言要点**：
- **整数与浮点数的混合运算**：level、power这些是int，2.0f是float，运算时int会自动提升为float
- **强制类型转换**：最后用`(int)`把浮点数转回整数
- **rand()%N取随机数**：`rand()%16`产生0~15的整数，`rand()%16==0`就是1/16的概率

### 2.7 战斗子状态机

战斗系统内部还有一个**子状态机**，管理回合内的执行顺序：

```
STATE_INTRO    → 开场介绍
STATE_COMMAND  → 玩家菜单（战斗/道具/精灵/逃跑）
STATE_FIGHT    → 技能选择
STATE_EXECUTE  → 执行阶段（攻击动画、伤害显示）
STATE_MESSAGE  → 消息暂停（等待按键）
```

这个子状态机和外面的游戏状态机是**同一种设计模式**，只是粒度更细。体现了"分层状态机"的思想。

在执行阶段（STATE_EXECUTE），我用**帧计时器**驱动攻击动画：

```c
animTimer++;  // 每帧+1
if (animPhase == 0 && animTimer > 30) {  // 30帧后
    // 计算玩家伤害，显示文字
    animPhase = 1; animTimer = 0;  // 进入下一阶段
} else if (animPhase == 1 && animTimer > 30) {
    // 敌方反击...
}
```

60FPS下30帧=0.5秒，通过帧计数来控制节奏，比用真实时间简单直观。

### 2.8 玩家移动与碰撞检测

玩家在地图上移动的核心流程：

```c
void UpdatePlayer(Player *p, Map *map, float dt) {
    // 1. 读键盘输入
    Vector2 input = {0};
    if (IsKeyDown(KEY_W)) input.y -= 1;  // W键向上
    if (IsKeyDown(KEY_S)) input.y += 1;  // S键向下
    // ...
    
    // 2. 对角线归一化
    if (input.x != 0 && input.y != 0) {
        float inv = 1.0f / sqrtf(2.0f);
        input.x *= inv;  // 斜方向减速到1倍速
        input.y *= inv;
    }
    
    // 3. 计算位移（位移 = 方向 × 速度 × 帧时间）
    float dx = input.x * speed * dt;
    float dy = input.y * speed * dt;
    
    // 4. AABB碰撞检测与修正
    Rectangle collide = playerCollideRect(p);
    collide.x += dx;
    for (int i = 0; i < solidCount; i++)
        resolveCollisionX(&collide, solidRects[i]);
}
```

**两个关键点**：
第一，**对角线归一化**——如果同时按W和D，合速度会是√2倍，必须除以√2来保持正常速度。
第二，**AABB碰撞检测**（轴对齐包围盒检测）——用四个不等式判断两个矩形是否重叠。

---

## 第三章：程序运行完整流程演示（约1.5分钟）

现在我从头到尾走一遍程序的运行过程：

**第一步**，main函数启动，创建960×640的窗口，设置60FPS，调用InitGame初始化。InitGame做了三件事：加载中文字体、加载宝可梦数据库、处理人物立绘图片的绿幕抠图。

**第二步**，进入主循环。初始状态是GAME_TITLE标题界面，屏幕上显示"POKEMON FIRE RED"标题文字和两个按钮——"开始游戏"和"制作人员"。

**第三步**，玩家点击"开始游戏"，currentState切换到GAME_STORY。屏幕上逐句显示4句故事台词："这个世界有着神奇的生物"→"它们被称之为精灵"→"与我们相伴共生并与一起战斗"→"开启这段旅程吧"。玩家每点一次鼠标推进一句。

**第四步**，故事结束后自动进入GAME_DIALOGUE对话界面，显示一位博士的立绘和欢迎语。玩家点击后，程序加载世界地图，currentState切换到GAME_WORLD。

**第五步**，在世界地图中，玩家用WASD键移动角色，Shift键加速跑动。UpdatePlayer函数每帧检测输入、更新位置、做碰撞检测防止穿墙。摄像机平滑跟随玩家移动。

**第六步**，玩家可以：
- 按B键进入遭遇战，遇到野生宝可梦
- 按E键与地图上的NPC馆主对战
- 按P键打开宝可梦图鉴，上下键浏览
- 踩楼梯切换楼层，踩门进入室外

**第七步**，进入战斗后，InitBattle初始化双方宝可梦——我方是阿勃梭鲁（Lv30），对手是化石翼龙（Lv28）。战斗状态机从开场介绍开始，玩家选择"战斗"后进入技能菜单，选技能后执行伤害计算——涉及命中判定、属性克制查表、随机数波动、暴击判定，最后显示伤害信息。敌方也会反击。一方HP归零后战斗结束，玩家回到地图继续探索。

**第八步**，按ESC返回标题界面，点击窗口关闭按钮。程序调用CloseGame释放所有资源：卸载纹理、释放字体、释放数据库内存，最后关闭窗口。程序正常退出。

---

## 结束语（约30秒）

以上就是我项目的全部介绍。

总结一下，这个C语言游戏项目综合运用了**结构体、枚举、指针、动态内存分配、文件读写、二维数组、状态机、字符串操作**等核心C语言知识。通过这个项目，我深入理解了这些概念在实际编程中是如何配合使用的，也对游戏开发的"更新-绘制主循环"模式有了切身的体会。

谢谢各位老师，请提问。

---

